/* The script system
 * All command is loaded and controled here
 */

webvn.add('script', function (s) {

var script = {};

// container of commands
var command = [];

// Private function

function getOpt(cmd) {



}

/* Parse a command into a well-formed one
 * video -d=2000 opening.avi
 * will become
 * {name:'video', option:{'duration':2000}, value:'opening.avi'}
 */
function parse(cmd) {



};

return script;

});